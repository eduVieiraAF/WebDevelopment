// function declaration
function fun1(x, y) { return x + y }

// function expression
const fun2 = function (x, y) { return x + y }

// named function expression
const fun3 = function fun3(x, y) { return x + y }
        // not commonly used
        // can be handy when debugging